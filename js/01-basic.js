/*
 * Функции
 * - Функциональные выражения
 * - Аргументы и параметры
 * - Возврат значения
 */

const add = function (a, b) {
    console.log(a);
    console.log(b);

    const result = a + b;
    console.log('Выполняется');


    return result;
};

const r1 = add(2, 3);
console.log(r1);


/*
Стек вызовов
*/

